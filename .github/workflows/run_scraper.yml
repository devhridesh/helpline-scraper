name: Run Corporate Helpline Scraper

on:
  schedule:
    - cron: '0 */4 * * *' # Har 4 ghante mein chalne ke liye
  workflow_dispatch: # Taaki aap manually bhi button dabaakar jab chahein run kar sakein

jobs:
  scrape_and_sync:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install google-generativeai supabase

    - name: Execute Extractor Script with Hidden Secrets
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      run: python secure_extractor.py